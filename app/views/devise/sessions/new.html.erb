

  <div class="border-form-div">
    <h2>Bienvenido</h2>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
      <%= f.email_field :email, :autofocus => true, :placeholder => 'Correo Institucional' %>
      <%= f.password_field :password, :placeholder => 'Contraseña' %>
      
      <%= f.submit "Ingresar",:class=>'btn btn-primario' %>
      <%= link_to "Registrarse", new_registration_path(resource_name), :class=>'btn btn-primario'   %>
    <% end %>
    <%#= render "devise/shared/links" %>
  </div> 
