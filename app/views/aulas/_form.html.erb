<%= form_for(@aula) do |f| %>
  <% if @aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aula.errors.count, "error") %> prohibited this aula from being saved:</h2>

      <ul>
      <% @aula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <%#= @aula.aleatorio(6) %> <!--ver metodo en el modelo-->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
